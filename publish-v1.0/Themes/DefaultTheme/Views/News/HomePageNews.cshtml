@model HomePageNewsItemsModel
@using Nop.Web.Models.News;
@if (Model.NewsItems.Count > 0)
{
    @*<div class="news-list-homepage">
        <div class="title">
            <strong>@T("News")</strong>
        </div>
        <div class="news-items">
            @foreach (var item in Model.NewsItems)
            {
                <div class="item">
                    <div class="news-head">
                        <a class="news-title" href="@Url.RouteUrl("NewsItem", new { SeName = item.SeName })">
                            @item.Title
                        </a> <span class="news-date">
                            -
                            @item.CreatedOn.ToString("D")
                        </span>
                    </div>
                    <div class="news-details">
                        @Html.Raw(item.Short)
                    </div>
                    <a href="@Url.RouteUrl("NewsItem", new { SeName = item.SeName })" class="read-more">
                        @T("News.MoreInfo")
                    </a>
                </div>
            }
            <div class="view-all">
                <a href="@Url.RouteUrl("NewsArchive")">
                    @T("News.ViewAll")
                </a>
            </div>
        </div>
    </div>*@
    <div class="home-new-title"><a href="@Url.RouteUrl("NewsArchive")" target="_blank">瑞翠快报</a></div>
    <ul>
        @for (int i=0;i<Model.NewsItems.Count;i++)
            {
                    if (i==0)
                    { 
                        <li class="first">
                   
                    <a target="_blank" href="@Url.RouteUrl("NewsItem", new { SeName = Model.NewsItems[i].SeName })">
                        <span class="dot">·</span>
                        @Model.NewsItems[i].Title
                        <i class="iconfont">&#xf02af;</i>
                    </a>
                </li>
                    }
                    else
                    { 
                     <li >
                   
                    <a target="_blank" href="@Url.RouteUrl("NewsItem", new { SeName = Model.NewsItems[i].SeName })">
                        <span class="dot">·</span>
                        @Model.NewsItems[i].Title
                        <i class="iconfont">&#xf02af;</i>
                    </a>
                </li>
                    }
        }
            </ul>

}
